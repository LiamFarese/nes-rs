[package]
name = "nes-rs"
version = "0.1.0"
edition = "2024"
license = "MIT"
repository = "https://github.com/LiamFarese/nes-rs"

[dependencies]

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
